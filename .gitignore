# Claude Code local settings / memory / project context
.claude/
CLAUDE.md
Explanation.md
Q&A.md
# OS cruft
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo

# Slurm logs
*.log
slurm-*.out
umap_atlas_*.log

# Local scratch
_smoke*.html

# Local backups (e.g. MG08_umap_atlas.html.bak) — keep out of the repo
*.bak

# NOTE: MG08_umap_atlas.html is now PUBLISHED via GitHub Pages (final artifact),
# so it is intentionally tracked. Regenerate via umap_atlas.py (SEED=42).
